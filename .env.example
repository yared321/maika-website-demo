# Copy to `.env.local` for `./run.sh` / `npm run dev` (face-assess proxy only).
# For Cloudflare Pages Functions locally: copy to `.dev.vars` in the repo root.

MAIKA_RPPG_UPSTREAM=https://your-rppg-service.example.com
MAIKA_PUBLIC_KEY=
MAIKA_CAPTCHA_TOKEN=
CORS_ALLOW_ORIGIN=*
